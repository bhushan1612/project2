import { NummaxlengthPipe } from './nummaxlength.pipe';

describe('NummaxlengthPipe', () => {
  it('create an instance', () => {
    const pipe = new NummaxlengthPipe();
    expect(pipe).toBeTruthy();
  });
});
